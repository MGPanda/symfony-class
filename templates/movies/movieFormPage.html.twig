{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('styles/movieFormPage.css') }}" type="text/css" rel="stylesheet"/>
{% endblock %}

{% block body %}
    <div class="p-movie-form">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Enviar</button>
        {{ form_end(form) }}
    </div>
{% endblock %}